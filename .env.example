# Optional: production-grade road routing via Mapbox Directions.
# Without this, the app uses the public OSRM demo server (no key, best-effort),
# and falls back to straight-line drive estimates if routing is unreachable.
MAPBOX_TOKEN=
